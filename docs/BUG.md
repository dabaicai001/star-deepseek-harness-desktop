# BUG 清单

> 以下 4 条已全部修复,随 v0.37.0 (2026-07-31) 发布。详见 CHANGELOG.md [0.37.0]。

1. ~~设置审计日志中的详情没有值,mysql 等查询语句没有也记录,现在只有 ssh 的审计有详细记录,并且审计日志加入条数上限,超过上限就删除最早的~~
   - ✅ 已修复:写入后自动修剪仅保留最新 5000 条;db/ssh/docker/sftp/ai 全事件补全 detail;SFTP 多选/拖拽上传补埋点;修复 db connect 审计端口写死 3306
2. ~~ES 没有右侧边栏,这个要加上,还有右侧边栏这个是公共组件吗?为什么 ES 没有这个侧边栏,如果不是公共组件要把他统一成公共组件~~
   - ✅ 已修复:右侧边栏本来就是公共组件 `src/components/layout/RightPanel.vue`(Db/Redis/Docker/Excel/SSH 共用),ES 已接入(EsOverview 集群仪表盘 + AI 助手 tab)
3. ~~AI 助手功能,没有引导功能需要加上一个,还有上面的后台静默看看有没有真正实现,有没有什么 bug,还有这个 AI 助手可以整体再重构一版页面,让其变得更实用~~
   - ✅ 已修复:引导弹窗 + 空状态快捷问题已有;后台静默已真实生效(oneshot exec + cwd 跟踪 + 可中断);修复引导文案教 @/# 而内嵌面板不支持、chips 硬编码中文、静默开关跨 tab 不同步;assistant 消息接入 Markdown 渲染、超长工具结果可展开
4. ~~红框部分按钮移动到上面,然后点击这个按钮不应该弹出输入网址,而是直接在 Starhub 中开启一个子页面,和浏览器一样,并且考虑是否要嵌入浏览器内核,而且现在输入网址,会出现错误报 404 而无法使用服务器的浏览器~~
   - ✅ 已修复:按钮移到终端工具栏;点击直接开 `web/:id` 子页面(地址栏 + Tauri 子 webview 嵌入主窗口);新增 `ssh_add_web_proxy_forward` 改写 HTTP Host 头,修复虚拟主机站点 404。限制:HTTPS 目标暂不支持,降级直连并提示
