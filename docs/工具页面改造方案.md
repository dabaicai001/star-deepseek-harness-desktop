# StarHub 工具页面统一改造方案

> 版本:v0.116.10 基线 | 审查方式:5 路并行代码审查(全部工具页逐行通读 + 后端命令面/ACL/sidecar 三层取证),所有结论均有 `文件:行号` 证据
> 范围:工具面板(资产列表)、SSH 终端、SFTP、内嵌浏览器、关系型数据库工作台、Redis 工作台、Elasticsearch 工作台、Docker 工作台、Broker 视图、文件树/文件查看器、沙箱桌面面板、Android 面板
> 路径缩写:`CN` = `vendor/deepseek-harness/packages/starhub/client-nav/src/client`

---

## 0. 总体结论

工具页整体「能连、能看、能基本操作」,但存在四个系统性问题:

### ① 后端能力明显领先前端,大量已建成能力零接线(最高性价比缺口)

后端(Rust command + ACL + Go sidecar)三层就绪但前端零调用的能力:

| 域 | 已就绪未接线的能力 | 证据 |
|---|---|---|
| SSH | 端口转发 5 命令(local/web-proxy/remote/list/remove)、会话 attach/detach | `src-tauri/src/commands/ssh.rs:1024-1109`、`:513`、`:567` |
| SFTP | 内置读/写文件(`sftp_read`/`sftp_write`)、限速(`sftp_set_speed_limit`)、断点续传 | `src-tauri/src/commands/sftp.rs:100,127,368`、`src-tauri/src/sftp/transfer.rs:904-1011` |
| 数据库 | 12+ 命令:insert_row / delete_rows / export_data / rename_table / get_table_meta / CH partitions·table_stats·merge_tree_info / backup / restore / list_backups | `src-tauri/permissions/commands.toml:52-151` |
| Redis | 9 命令:info / slowlog_get / slowlog_reset / scan_all / bigkey_scan / memory_analysis / subscribe / unsubscribe / test | `src-tauri/src/main.rs:330-339`、`sidecar/adapters/handlers.go:100-109` |
| ES | 10 命令:cluster_stats / count / get_document / index_document / update_document / delete_document / bulk_index / export_json / scroll_search / test | `src-tauri/src/main.rs:345-359`、`handlers.go:116-130` |
| Docker | Compose 六命令(list/ps/up/down/logs/config,ACL 已授权)、pause/unpause(沙箱已在用)、prune、日志 follow、create/commit/copy/build | `src-tauri/src/main.rs:422-427`、`sidecar/adapters/docker_manage.go:170-184`、`docker.go:284` |
| 本机文件 | create_directory / copy_file / move_path / remove_path(文件树 UI 未接) | `src-tauri/src/commands/local.rs:428-474` |
| 资产模型 | `group_id / tags / favorite / last_used_at` 字段存在,前端只写空值、从未读取 | `CN/store.ts:25-30`、`CN/NewConnectionDialog.tsx:285-287` |

**这批项是「补 UI 接线即可交付」的低成本高价值缺口,应作为功能补全的第一梯队。**

### ② 一批 P0 级「静默错数据/误操作」缺陷,测试全绿只能靠真实使用暴露

| # | 缺陷 | 位置 |
|---|---|---|
| P0-1 | **改列/索引对话框搜索后索引错位**:用 filtered 下标操作 edits 数组,搜索后点 × 会删错列/删错索引 | `CN/DbTableDialogs.tsx:348-372,435-465,593-615,668-693` |
| P0-2 | **数据网格未保存编辑被静默丢弃**:翻页/排序/筛选/WHERE/刷新都触发 `load` → `setDirty(new Map())` | `CN/DbDataGrid.tsx:260,270-272,536-544` |
| P0-3 | **单元格编辑改类型**:按原值猜类型,VARCHAR 里的手机号/长 ID 被写成 number(超 2^53 精度丢失);空串一律变 NULL | `CN/DbDataGrid.tsx:87-89,431-435` |
| P0-4 | **Redis CLI 执行 SELECT 造成库漂移**:UI 显示 db0,实际连接已切走 → FLUSHDB 确认文案写 db0、实际清 db5(与 v0.116.10 修的 AI 侧事故同源,但工作台是持久连接,切库生效,风险更大) | `CN/redis/RedisWorkbench.tsx:288,290-299,380-394,414-426`;`sidecar/adapters/redis.go:470-488,993-1001` |
| P0-5 | **Redis 结构类型 TTL 假保存**:hash/list/set/zset 的 TTL 输入不下发,UI 却显示已生效 | `CN/redis/RedisValueEditor.tsx:156-162` |
| P0-6 | **Redis list 删除语义错误**:`LREM key 0 <索引>` 删的是值恰好等于索引数字的元素 | `CN/redis/RedisValueEditor.tsx:58-65,233-241,263-268` |
| P0-7 | **ES 翻页 stale closure**:先 setState 再同步执行,查询用旧 from → 页码与数据永久错位一页 | `CN/es/ElasticsearchWorkbench.tsx:144-163,165-177` |
| P0-8 | **文件保存截断丢数据**:读窗口 256KB,保存写回已加载内容,`truncated` 不参与保存门禁;截断提示文案(8KB)与实际行为(256KB)不符 | `CN/file-tree/FileInfoDialog.tsx:72,156,172,215-217`;`CN/file-viewer/FileViewerOverlay.tsx:161,203` |
| P0-9 | **二进制文件可被打开并写坏**:后端 lossy 解码,UI 直接进 textarea 且允许保存 | `src-tauri/src/commands/local.rs:381`;`CN/FileInfoDialog.tsx:122-128` |
| P0-10 | **Docker「显示全部」不生效**:勾选不重拉,后端 `all=false` 只返回运行中容器 → 停止容器永不出现;空态误报「暂无容器」;测试 mock 忽略 `all` 参数导致假绿 | `CN/docker/DockerWorkbench.tsx:181-192,238,388-391,499-505` |
| P0-11 | **Docker 删除容器硬编码 `rm -f`**:确认文案未提示会强杀运行中容器 | `CN/docker/DockerWorkbench.tsx:245,250` |
| P0-12 | **Broker 未配 host 永久 loading**:五张卡永久不可点,无错误提示 | `CN/broker/BrokerView.tsx:41-48,66` |
| P0-13 | **SQLite/MSSQL 工作台实际不可用**:connect 走 `db_mysql_connect` + 3306,硬校验 host/username,连接对话框连类型选项都没有(后端已就绪) | `CN/DbWorkbench.tsx:257-276,435`;`CN/NewConnectionDialog.tsx:26-36` |
| P0-14 | **SFTP 已连接后所有操作错误不可见**:`error` 只在未连接态渲染;RETRY 按钮只清错误不重试 | `CN/terminal/SftpPanel.tsx:250-252,303-305,367,396-401` |
| P0-15 | **内嵌浏览器右键被彻底吞掉**:网关注入脚本 preventDefault 后 postMessage,壳页与 React 组件均无消费者;`_blank`/Ctrl 点击同样被吞 | `src-tauri/src/ssh/web_gateway.rs:1100-1116`、`src-tauri/src/browser/web_shell.rs:79-125` |
| P0-16 | **沙箱销毁/模板删除无确认**,一次点击即执行 | `CN/sandbox/SandboxPanel.tsx:160-167,198-205` |
| P0-17 | **Docker exec 终端两条死路**:启动失败静默(永久黑屏);会话结束/10 分钟空闲回收后弹层不关不提示 | `CN/docker/DockerExecTerminal.tsx:77,98-101,112-113` |

### ③ 右键菜单:覆盖率约 1/3,且两套实现并存

**已覆盖**(共享 `ContextMenu` 组件,`CN/ContextMenu.tsx`):资产行、文件树节点/搜索行、DB 库行、DB 表行、数据网格行。
**SFTP 自绘一套**(fixed 定位 + 手算视口夹紧,无 role="menu"、无键盘),未用共享组件:`CN/terminal/SftpPanel.tsx:516-548`。
**零覆盖**:SSH 终端(选中复制/粘贴/清屏/重连)、Redis(db 节点/key/文件夹)、ES(索引行/命中行/映射字段)、Docker(容器行/镜像行)、Broker(topic 行/卡片)、沙箱实例卡、Android 设备卡、执行记录行、数据网格单元格与列头、DB 列节点、查询标签、面包屑、传输任务行。

共享组件本身的能力缺口:`Menu` 原语支持 footer/selectedId/align/side/dense/compact/label 组标题/子菜单,包装层全部不透传(`CN/ContextMenu.tsx:64-72`);MenuItem 无快捷键提示字段;无 `aria-haspopup/aria-expanded`、无焦点管理、无 Shift+F10 键盘入口;「点击后保持打开」语义缺失(资产行靠 1.5s 定时器伪造「已复制」)。

### ④ 交互一致性债:「一个事实多处编码」

- **危险确认 3 套并存**:两段式按钮(删连接)/ 专用确认弹窗(SFTP)/ 无确认(沙箱销毁、执行记录清空、ExecRecordList 断开、文件覆盖保存)
- **原生 `window.confirm/alert` 散落各处**(DB 删表/清空表/导出成功、Redis 删 key/FLUSHDB、Docker 删容器/删镜像/prune),与设计系统弹层不一致
- **错误呈现 4 套**:`status+retryButton` / `errorBanner` / `statusLineError` / `bannerError`,各模块自定
- **预览态检测 4 种写法**:store `preview` / 直读 `__TAURI_INTERNALS__` / 按错误文案 includes 匹配 / 混为普通错误
- **加载态**:多处整块替换为「加载…」导致列表闪没(资产列表、Docker 容器/镜像);子类切换无脑重拉
- **键盘/Esc**:工具面板无 Esc 关闭无焦点陷阱(它是 `aria-modal` dialog);沙箱两个自研弹窗无 Esc;DB 三个对话框无 Esc;Docker 日志弹层无 Esc;而 StarHubOverlay/连接卡有 —— 同产品两套习惯
- **死代码/漂移**:`terminal/WebBrowser.tsx` 整组件无生产挂载(生产走 Rust 壳页,两套 postMessage 协议互不匹配);`exportError` 死状态(DbWorkbench);`redisInfo`/`esCount`/`indexRowOf` 死封装;`assetWindowUrl` 重复死分支(`CN/sections.ts:189-192`);Redis Dashboard 分支不可达;`sandbox/android` 特判三处编码

---

## 1. 横向改造(先做地基,后续每页收益)

### 1.1 建设 StarHub 共享 UI 原语层(目前只有 ContextMenu 一个胶水组件)

新增/增强 `CN/components/`(名字示意):

1. **`ContextMenu` 增强**:透传 footer/align/side/dense/compact;支持 label 组标题与子菜单样板;MenuItem 加 `shortcut` 提示字段;补 `aria-haspopup/aria-expanded`、打开移焦/关闭还焦、Shift+F10 键盘入口、「⋮」备用触发钮;统一「一个面板一个菜单实例 + target 状态」范式(资产行现在每行一个 Menu portal)。**SFTP 自研菜单迁移过来,消灭第二套实现**。
2. **`ConfirmDialog`**:统一危险确认(两段式 + 弹窗二合一),支持输入名称二次确认(用于 FLUSHDB/删索引/销毁沙箱)。替换全部 `window.confirm/alert`。
3. **`StatusBanner` / `EmptyState` / `LoadingBlock`**:统一错误(可关闭、带重试)、空态(带 CTA)、加载(骨架/spinner)四套样式为一。
4. **预览态单一信号**:全部走 store 的 `preview`,禁止按错误文案匹配。
5. **i18n 基建**:client-nav 全目录无 i18n 层,面向用户文案全硬编码中文(且 ES 侧混入英文)——按 AGENTS.md 约定补齐。

### 1.2 右键菜单全量覆盖清单(共享组件就绪后逐页接入)

| 对象 | 建议菜单项 |
|---|---|
| SSH 终端选中文本 | 复制 / 全选 / 用选区搜索 |
| SSH 终端空白区 | 粘贴 / 清屏 / 查找 / 重新连接 / 新建标签 |
| 数据网格单元格 | 复制值 / 设为 NULL / 设为空串 / 复制列名 |
| 数据网格列头 | 复制列名 / 隐藏列 / 升序·降序 |
| 数据网格行(现有) | 补:删除行 / 复制为 UPDATE·DELETE·JSON·CSV |
| DB 表行(现有) | 补:复制表名 / 生成 SELECT·INSERT / 重命名表 / 导出 / 在新查询中打开 |
| DB 列节点(现零交互) | 复制列名 / 生成 SELECT 列清单 |
| Redis db 节点 | 刷新该库 / 清空该库(强确认)/ 键数 |
| Redis key | 打开 / 重命名 / 复制 key 名 / 复制值 / 设置 TTL / 持久化 / 删除(danger) |
| Redis 文件夹 | 按前缀删除(danger)/ 展开全部 / 复制前缀 |
| ES 索引行 | 打开检索(预填)/ 查看映射 / 复制索引名 / 删除(danger) |
| ES 命中行 | 查看 _source / 复制 _id / 复制文档 JSON / 编辑 / 删除 |
| Docker 容器行 | 启动/停止/重启/暂停/删除 + 日志/终端/统计/详情 + 复制容器 ID(收纳现有 7 个图标按钮,解决窄窗口溢出) |
| Docker 镜像行 | 复制 tag / 复制 id / 删除(danger) |
| Broker topic 行 | 复制 topic 名 / 查看分区(待后端) |
| SFTP 行(现有自绘) | 迁移共享组件 + 补:属性/权限 / 以文本编辑 / 在终端打开 / 复制名称 |
| 沙箱实例卡 / 模板卡 / Android 设备卡 / 执行记录行 | 对应操作 + 复制 id |
| 资产行(现有) | 补:测试连接 / 另存为副本 / 复制主机名(子菜单) |
| 子类行 | 刷新本子类 / 折叠全部 |

### 1.3 键盘与弹层一致性规范

- 所有 `role="dialog"` 必须有 Esc 关闭 + 焦点陷阱 + 打开移焦(工具面板、DB 三对话框、Docker 日志/exec 弹层、沙箱两弹窗当前都缺)
- 文件列表/树统一 `role="tree"/"option"` + tabIndex + Enter/Delete/F2/方向键(SFTP 文件行、文件树、ES 表格行当前是纯 `div onClick`)

---

## 2. 分页面改造要点

### 2.1 工具面板 / 资产列表(`StarHubToolWorkspace.tsx`)

- **P0**:右键「删除」语义修正(现在只是打开编辑对话框,要么改名「管理连接」要么菜单内两步确认)(`:114,150,357`)
- **P1**:资产搜索框 + 排序 + 分组/标签/收藏过滤(数据模型已有,`:306,346-361`);资产行补「测试连接」入口;`local` 资产无归宿(路由返回 'local' 但无子类收留,`sections.ts:54-65`);连接健康状态指示
- **P2**:子类切换加 TTL 缓存避免列表闪 loading(`:204`);「已复制路径」在文件树菜单永久驻留 bug(`FileTreePanel.tsx:77,190`)

### 2.2 SSH 终端(`terminal/SshTerminalOverlay.tsx`)

- **P0**:断开后状态文案撒谎(永远「连接中」)、补「断开/重连」入口(`:516-521,706,754`)
- **P1**(对标 Xshell/Tabby):终端搜索(引 `@xterm/addon-search` + Ctrl+F);复制/粘贴(Ctrl+Shift+C/V + 右键);右键菜单(现 0 处);**端口转发 UI(后端 5 命令已就绪)**;会话日志录制;OSC 0/2 标题上报;多标签/分屏(现 tab 行是写死 span);会话保持(attach/detach 后端已有)
- **P1 交互**:快捷命令点击即执行破坏性命令无确认(`:621-625`);广播无二次确认、会话标题用内部 session id(`:609`);错误横幅不可关闭(`:723`)
- **P2**:改字体/字号即时生效(现需重开);终端设置扩充(滚回行数、粘贴保护);侧栏宽度持久化

### 2.3 SFTP(`terminal/SftpPanel.tsx`)

- **P0**:F1 错误不可见 + RETRY 假重试(见 P0-14);跟随终端开关持久化写了不读(`:45,266` vs `:92`)
- **P1**:属性/权限列与 chmod(后端已返回 permissions/uid/gid/modified,行内只渲染 name/size,`:484-486`);修改时间列 + 点击排序;**内置文本编辑(`sftp_read/write` 后端已有)**;限速 UI(`sftp_set_speed_limit` 已有);同名冲突策略;「在终端中打开该目录」;键盘操作;传输任务行加进度条/速度、失败任务不 4 秒自动消失;上传后不靠 `setTimeout(2000)` 猜刷新(`:302`)
- **P2**:本地面板/双栏 + 拖出下载(对标 WinSCP,大功能);文件夹同步;图标语义错配修正(`:417,427,484`)

### 2.4 内嵌浏览器(`terminal/WebBrowser.tsx` + Rust 壳页)

- **P0**:右键/`_blank` 点击被吞(见 P0-15);**二选一消灭双实现**(React 组件零生产挂载,其测试对生产链零保护)
- **P1**:「在外部浏览器打开」(后端 `open_external_url` 已就绪,`ssh.rs:1220-1230`);加载失败/证书错误提示;前进/后退可用性
- **P2**:壳页 iframe 补 sandbox(现无,存在顶窗劫持面,`web_shell.rs:78`);地址栏与真实页面同步;停止加载按钮

### 2.5 关系型数据库工作台(`DbWorkbench.tsx` / `DbDataGrid.tsx` / `DbTableDialogs.tsx` / `SqlEditor.tsx`)

- **P0**:对话框索引错位、未保存编辑丢弃、类型改写、SQLite/MSSQL 接通、多语句保留全部结果集(现只留最后一条,EXPLAIN 不拆分必报错)(详见 P0-1/2/3/13;多语句 `DbWorkbench.tsx:619-631`)
- **P1**:
  - SQL 编辑器:「执行选中/执行当前语句」(现在整篇执行);行号、查找替换、括号匹配;多结果集标签;危险语句(DELETE/UPDATE 无 WHERE)确认;查询历史按资产/库隔离(现全局混一个 key)
  - 数据网格:行级 DML(新增行/删除行/复制行,后端命令已授权零调用);列筛选操作符(现只有 `col = ?` 单值等值,用户按 Navicat 直觉输 `%abc%` 查不到);列显示管理;行详情/大字段查看器;数据加载竞态防护;导出含筛选透传修正
  - 树:视图/物化视图与表分组(list_tables 已返回 type 被丢弃,`DbWorkbench.tsx:488`);表右键扩充;库级操作(新建库/删除库);展开库时列预取风暴收敛(几百表并发 `list_columns` 无上限,`:790-807`)
  - 对话框:三处 DDL 应用加「SQL 预览」步骤;PG/CH 放开改列/索引(现 `supportsAlter = dbType==='mysql'` 一刀切,ddlGenerator 写死 MySQL 反引号)
  - Dashboard:切库联动刷新;自动刷新;会话明细 KILL 操作
- **P2**:大结果集保护(execute 无行数上限,全量进内存走 IPC);复制为 INSERT 方言修正(反引号在 PG/CH 非法);分页首页/末页/跳页;480px 网格高度硬上限

### 2.6 Redis 工作台(`redis/`)

- **P0**:SELECT 库漂移、结构类型 TTL 假保存、list 删除语义错(见 P0-4/5/6)
- **P1**:打开大 key 无保护(百万成员 hash 全量进内存渲染,无元素数上限);新建 key 弹窗补类型选择器(`NewKeyDraft.type` 是死状态)并改用 `redisQuote`;**stream 类型支持**(现打开 stream key 会按 hash 渲染、删除下发非法 LREM、保存下发 HSET —— 误写风险);键行显示 TTL/大小;批量删除(后端已支持 keys[]);右键菜单(0 覆盖);**接线慢查询/内存分析/bigkey/pub-sub/INFO(9 命令三层就绪)**;危险确认改设计系统模态 + FLUSHDB 输入库名二次确认
- **P2**:键树/结构表虚拟化;`countLeaves` 每渲染递归计数(O(键数×文件夹数));CLI 历史/补全/输出清空;db 列表按 INFO 动态发现(现硬编码 0-15)

### 2.7 Elasticsearch 工作台(`es/`)

- **P0**:翻页 stale closure(见 P0-7);「所有索引」下发字面量 `_all`(ES 7+ 已移除,改 `*` 或省略)(`ElasticsearchWorkbench.tsx:158`)
- **P1**:**文档 CRUD/bulk 导入/导出 JSON/scroll 深分页/cluster_stats(10 命令三层就绪)**;聚合结果渲染(后端已解析返回,前端只 stringify hits 把 aggs 静默丢弃);索引设置/映射编辑(创建弹窗只提交名称,mappings/settings 参数从 UI 不可达);命中行操作(查看 _source/复制/编辑/删除);DSL 编辑器换 CodeMirror(仓库已有);索引列表搜索/排序/分页;effect 依赖 `[asset]`→`[asset.id]`(父渲染导致断连重连,`:117`)
- **P2**:mapping 三层以上嵌套丢失(`fieldRow` 只归一化一层);删除索引输入名称确认;中英混杂文案

### 2.8 Docker 工作台(`docker/`)

- **P0**:「显示全部」不生效 + 空态误报 + 测试 mock 漂移(见 P0-10);删除恒 `-f`(P0-11);exec 终端死路(P0-17)
- **P1**:**Compose 项目 UI(六命令+ACL 全就绪,零前端引用 —— 全场最大闲置能力)**;暂停/继续按钮(概览卡显示「暂停」计数却动不了);日志 follow/tail 选择/下载/搜索(现 tail 写死 200 手动刷新);inspect 详情面板(`dockerInspectContainer` 已封装无人调用);连接失败重连入口(现只能关窗重开);行操作 in-flight 防重;右键菜单(0 覆盖,同时解决一行 7 图标按钮溢出)
- **P2**:卷/网络管理(需先补 sidecar);批量操作与搜索过滤;统计实时化(复用 Broker 的折线 DashboardCard,两组件现重复造轮子);状态徽章英文直出与 created/restarting/dead 归错样式

### 2.9 Broker 视图(`broker/`)

- **P0**:未配 host 永久 loading(见 P0-12)
- **P1**:刷新重入保护 + 请求定序(30s 轮询可能旧响应覆盖新响应);展示已解析却被丢弃的 `channelList`;topic 钻取页;UI 内「测试连接」(已就绪);展示 `observedAt`;右键菜单(0 覆盖)
- **P2(需 sidecar 扩容)**:消息生产/消费浏览、consumer group 与 lag、topic 增删 —— kafka-go 需引入 Writer/OffsetFetch/ConsumerGroups,NSQ 需接 `/topic|/channel` HTTP API

### 2.10 文件树 / 文件查看器(`file-tree/`、`file-viewer/`)

- **P0**:保存截断与二进制写坏(见 P0-8/9)
- **P1**:接通后端已有的新建/重命名/删除/复制/移动(`local.rs:428-474`);刷新按钮;隐藏文件开关(字段已存在未用);搜索防抖(现每次按键发一次内容搜索);保存后目录缓存失效;read 模式保存补外部修改检测(edit 模式有,两模式不对称)
- **P2**:文件查看器升级编辑器(语法高亮/行号/查找/Ctrl+S);空目录空态;大小/修改时间列

### 2.11 沙箱 / Android 面板(`sandbox/`、`android/`)

- **P0**:销毁/删模板无确认(见 P0-16)
- **P1**:沙箱:刷新按钮 + 轮询(AI 建/删后列表不更新);新建实例入口(现空态只写「让 AI 创建」);模板构建按钮(`desktop_build_template` 已存在,UI 只显示「未构建」);实例详情(容器 id/创建时间/资源)
- **P1**:Android:设备详情(版本/分辨率/电量,AI 工具已有能力);无线调试配对 UI(`android_wireless` 仅 AI 路径);错误分支补重试按钮;自动刷新/插拔感知
- **P2**:回放帧导出/分页;两面板停止复用沙箱 CSS Module

---

## 3. 分阶段实施路线

### Phase 1 — P0 缺陷修复(约 17 项,以「静默错数据/误操作」为主,先行止血)
按 §0-② 表逐项修。重点:P0-1/2/3(DB 数据正确性)、P0-4/5/6(Redis 数据正确性)、P0-7(ES 翻页)、P0-8/9(文件保存)、P0-10/11/17(Docker)、P0-13(SQLite/MSSQL 接通或明确剔除)、P0-14(SFTP 错误可见)、P0-15(浏览器右键)、P0-16(沙箱确认)。
同步补测试:Docker mock 与后端契约漂移(`docker-workbench.client.spec.tsx:81`)、ES 翻页、DB 对话框搜索后操作、Redis list 删除。

### Phase 2 — 后端能力接线(纯前端成本,功能增量最大)
SSH 端口转发 UI、SFTP 内置编辑/限速/属性列、DB 行级 DML/导出/重命名/表元信息/备份还原、Redis 慢查询/内存分析/pub-sub、ES 文档 CRUD/bulk/scroll/聚合渲染、**Docker Compose 项目 UI**/pause/日志 follow、文件树文件操作、资产分组/标签/收藏、沙箱构建按钮。

### Phase 3 — 交互一致性(共享原语层 + 右键菜单全量覆盖)
§1.1 原语层 → §1.2 右键覆盖表逐页接入(含 SFTP 菜单迁移)→ §1.3 键盘/弹层规范 → 消灭 window.confirm/alert、4 套错误样式、4 种预览态检测 → i18n 基建。

### Phase 4 — 对标竞品的大功能(按需排期)
SSH 多标签/分屏/会话日志、SFTP 双栏/同步、DB 导入链路(后端也需补)/执行计划可视化/表设计器、Broker 读写能力(sidecar 扩容)、Docker 卷/网络/资源图表。

---

## 4. 测试补强建议

- `tests/sftp-panel.client.spec.tsx` 现仅 3 条用例,右键菜单/删除/传输/错误可见性/键盘全裸奔
- `tests/web-browser.client.spec.tsx` 全部针对未挂载的 React 组件,对生产链零保护 → Phase 1 决定唯一实现后重写
- 各 P0 修复必须带回归用例(重点:数据正确性类 P0 都是「测试全绿但生产出错」型)

---

*审查产出:本文档。审查过程只读,未修改任何代码。*
